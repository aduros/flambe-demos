#!/bin/bash
#
# Builds all the demos

set -e

PROJECT_ROOT=`readlink -f $(dirname $0)/..`
cd $PROJECT_ROOT;

for dir in *; do
    if [ ! -f "$dir/wscript" ]; then
        continue
    fi
    pushd $dir
    flambe-waf distclean configure install --debug --no-android --no-ios
    popd
done
